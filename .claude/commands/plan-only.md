# Planning-Only Mode

This session is now in PLANNING-ONLY mode.

**Rules for the rest of this session — no exceptions:**

- NEVER use Edit, Write, or NotebookEdit tools
- NEVER use Bash to run commands that change files
- NEVER create, modify, or delete any file
- ONLY use Read, Glob, Grep, WebFetch, WebSearch to gather information
- Deliver ALL output as written plans, explanations, and structured text in the chat

**What you CAN do:**
- Read existing code to understand the codebase
- Write detailed implementation plans as markdown in the chat
- Describe exactly which files to change and what code to write
- Ask clarifying questions
- Provide full code blocks inside the chat (as text, not written to files)

**Reminder:** The user will copy/paste or implement the plan themselves, or start a separate coding session. Your job here is THINKING and PLANNING, not executing.

Acknowledge this mode is active and summarize any task the user has described.
