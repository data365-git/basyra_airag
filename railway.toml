[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
preDeployCommand = "node scripts/fix-migration.cjs && npx prisma migrate deploy"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
